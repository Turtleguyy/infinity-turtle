// Generated by CoffeeScript 1.8.0
(function() {
  var InfiniteTurtle;

  InfiniteTurtle = (function() {
    InfiniteTurtle.prototype.defaults = {
      pageSize: 20,
      loader: {
        symbol: 'circle',
        color: '#0bc6d4',
        width: '2px',
        url: ''
      }
    };

    function InfiniteTurtle(options) {
      this._options = $.extend(options, this.defaults);
      this.buildLoader();
    }

    InfiniteTurtle.prototype.buildLoader = function() {
      var borderColor, borderLeft, borderTop, borderWidth, classes, inlineCSS;
      borderWidth = "border-width: " + this._options.loader.width + ";";
      if (this._options.loader.symbol === 'infinity') {
        borderColor = "border-color: " + this._options.loader.color + ";";
        inlineCSS = "style='" + borderWidth + " " + borderColor + "'";
      } else {
        borderTop = "border-top-color: " + this._options.loader.color + ";";
        borderLeft = "border-left-color: " + this._options.loader.color + ";";
        inlineCSS = "style='" + borderWidth + " " + borderTop + " " + borderLeft + "'";
      }
      classes = "class='infinite-loader " + this._options.loader.symbol + "'";
      return $("<div " + classes + " " + inlineCSS + " />").appendTo('body');
    };

    return InfiniteTurtle;

  })();

  window.InfiniteTurtle = InfiniteTurtle;

}).call(this);
